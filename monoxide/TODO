TODO

much, including...

* UI
	* add zoom +,-,fit... to image plugins
	* treeview
		* add namespace support
		* add parameters and return value
		* add colors (e.g. icall, unsafe)
		* add icons (just like MD)
	* add printing support
	* a lot more...

* plugins
	* detect unsafe code (e.g. ldflda, pointers)
	* add monop-style support on class (C#)


IDEAS

* Inheritance graphs (dot);
* UML plugin (non-dot);
