/*
 * Copyright (c) 2004 DotNetGuru and the individuals listed
 * on the ChangeLog entries.
 *
 * Authors :
 *   Jb Evain   (jb.evain@dotnetguru.org)
 *
 * This is a free software distributed under a MIT/X11 license
 * See LICENSE.MIT file for more details
 *
 * Generated by /CodeGen/cecil-gen.rb do not edit
 * Wed Feb 23 03:33:52 Paris, Madrid 2005
 *
 *****************************************************************************/

namespace Mono.Cecil.Implem {

    using System;
    using System.Collections;

    using Mono.Cecil;
    using Mono.Cecil.Cil;

    internal class OverrideCollection : IOverrideCollection, ILazyLoadableCollection {

        private IList m_items;
        private MethodDefinition m_container;
        private LazyLoader m_loader;

        private bool m_loaded;

        public IMethodReference this [int index] {
            get {
                if (m_loader != null)
                    m_loader.ReflectionReader.Visit (this);
                return m_items [index] as IMethodReference;
            }
            set { m_items [index] = value; }
        }

        public IMethodDefinition Container {
            get { return m_container; }
        }

        public int Count {
            get {
                if (m_loader != null)
                    m_loader.ReflectionReader.Visit (this);
                return m_items.Count;
            }
        }

        public bool IsSynchronized {
            get { return false; }
        }

        public object SyncRoot {
            get { return this; }
        }

        public bool Loaded {
            get { return m_loaded; }
            set { m_loaded = value; }
        }

        public OverrideCollection (MethodDefinition container)
        {
            m_container = container;
            m_items = new ArrayList ();
        }        

        public OverrideCollection (MethodDefinition container, LazyLoader loader) : this (container)
        {
            m_loader = loader;
        }
        
        public void Add (IMethodReference value)
        {
            m_items.Add (value);
        }

        public void Clear ()
        {
            m_items.Clear ();
        }

        public bool Contains (IMethodReference value)
        {
            return m_items.Contains (value);
        }
        
        public int IndexOf (IMethodReference value)
        {
            if (m_loader != null)
                m_loader.ReflectionReader.Visit (this);
            return m_items.IndexOf (value);
        }

        public void Insert (int index, IMethodReference value)
        {
            m_items.Insert (index, value);
        }

        public void Remove (IMethodReference value)
        {
            m_items.Remove (value);
        }

        public void RemoveAt (int index)
        {
            m_items.Remove (index);
        }

        public void CopyTo (Array ary, int index)
        {
            if (m_loader != null)
                m_loader.ReflectionReader.Visit (this);
            m_items.CopyTo (ary, index);
        }

        public IEnumerator GetEnumerator ()
        {
            if (m_loader != null)
                m_loader.ReflectionReader.Visit (this);
            return m_items.GetEnumerator ();
        }

        public void Accept (IReflectionVisitor visitor)
        {
            visitor.Visit (this);
            IMethodReference [] items = new IMethodReference [m_items.Count];
            m_items.CopyTo (items, 0);
            for (int i = 0; i < items.Length; i++)
                items [i].Accept (visitor);
        }
    }
}
